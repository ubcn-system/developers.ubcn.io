---
title: API 시작하기
layout: /src/layouts/api/Layout.astro
sidebar:
    order: 2
tableOfContents: false
---

import Frame from "/src/components/Frame.astro";

## API 시작하기
<br/>

<Frame>
    단말기로부터 발생한 매출 데이터와 가맹점 시스템과 연계할 수 있는 부가서비스를 `B2B` API 형태로 제공합니다.
    <br/>

    카드 승인정보(API 제공)는 여신전문금융업법 및 개인정보보호법상 민감 정보에 해당할 수 있어

    `금융감독기관`의 **검사 대상 사업자** 에 한해 제공하고 있습니다.<br/><br/>

    이로 인해 아래 조건을 모두 만족해야만 서비스를 이용할 수 있습니다.
    - 금융감독원 검사 대상 법인
    - ISMS 또는 ISMS-P 인증 보유
    - 정보보호 책임자 지정 및 관리체계 보유
    - 별도 API 연동 계약 체결(유비씨엔)
</Frame>
<Frame>
    ### 지원 서비스
    - 거래내역
        - 거래내역 목록(5분 전)
    - 마감정보
        - 특정 일자의 마감된 거래들의 집계정보 목록
    - 상태정보
        - 단말기의 상태정보 (1시간 마다 단말기에서 발송하는 상태정보 제공) 목록

</Frame>
<Frame>
    ### 사전 준비 사항
    유비씨엔 API를 사용하기 위해서는 먼저, 가맹점 고유번호인 `company`와 암호화 된 `token`을 발급받아야 합니다. 해당 정보는 유비씨엔 관리자에게 문의하여 발급받을 수 있습니다.
</Frame>
<Frame>
    ### Request Schema
    <table>
        <thead>
            <th className={"text-center"}>Content-Type</th>
            <th className={"text-center"}>company</th>
            <th className={"text-center"}>token</th>
        </thead>
        <tbody>
        <tr>
            <td className={"text-center"}>application/json</td>
            <td className={"text-center blur-xs"}>12345</td>
            <td className={"text-center blur-xs"}>ABCDEFGHIJKLMNOPQRSTUVWXYZ</td>
        </tr>
        </tbody>
    </table>
</Frame>
<Frame>
    ### API 호출 방법
    API 호출 방법은 다음과 같습니다.
```ts

const API = axios.create({
  baseURL: `http://api.ubcn.co.kr/api`,
  headers: {
    "Access-Control-Allow-Origin": "*",
    "company": "{company}",
    "token": "{token}",
  },
});

// 서비스에 따라 `Method` 가 다를 수 있습니다.
const res = await API.get("{service}");
console.log(res.data);

// 프로세스가 제대로 완료되지 않은 경우 에러 코드가 존재합니다.
if (res.code != null) {
  return alert(res.decription);
}

```
</Frame>
